{
  "name": "bullishnwc",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-version": "node -e \"const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); const m=JSON.parse(fs.readFileSync('manifest.json','utf8')); m.version=pkg.version; fs.writeFileSync('manifest.json', JSON.stringify(m,null,4));\"",
    "prebuild": "npm run sync-version",
    "predev": "npm run sync-version",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
